import React from "react";

const Button = ({}) => {
  return (
    <button className="bg-subMain text-white px-8 py-4 font-medium rounded hover:text-black transitions ease-in-out">
      Watch
    </button>
  );
};

export default Button;
